# 2026-08-15

- 以 `prd/logo/icon.svg` 为单一来源，重新生成 Qraft 项目全套 Logo：
  - 更新 `assets/app-icon.svg` 为 1024×1024 主控 SVG（源自 `prd/logo/icon.svg`）。
  - 更新 `assets/source-icon.png` 为 1024×1024 光栅图。
  - 重新生成 `src-tauri/icons/` 完整 Tauri 图标集（PNG 多尺寸、icon.ico、icon.icns、Windows Store logos、Android mipmap、iOS AppIcon）。
  - 同步更新 `prd/logo/icon_32.png`、`icon_128.png`、`icon_256.png`、`icon_512.png`。
- 清理临时目录 `.icon_gen`、`.icon_gen_1024`。
- 在 `MEMORY.md` 中补充品牌图标单一来源约定。
