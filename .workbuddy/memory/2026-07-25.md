# 2026-07-25 — PRD 审查与 P0/P1 修正

## 任务
全面审查 `prd/` 20 篇设计文档（5 维度：内容准确性 / 结构完整性 / 语言规范性 / 格式一致性 / 可操作性），并落地用户指定的 P0/P1 修正。

## 已落地的修正（5 个文件，已回读 + Grep 校验）
- **03-tech-stack.md** §3.9：Rust 辅助依赖新增 `async_trait ^0.1`（P0 #1）。
- **05-rust-core-engine.md**：
  - `ToolInput` 加 `#[derive(Default)]`（P0 #2）；
  - `ToolError` 新增 `ToolNotFound(String)` + `OutOfMemory { size, max }`，`code()` 映射 `ERR_TOOL_NOT_FOUND` / `ERR_OUT_OF_MEMORY`（P0 #3 / P1 #4）；
  - executor `get()` 未命中从 `Internal` 改为 `ToolNotFound(tool_id)`；
  - 新增 `StreamingEntry` + `register_stream_tool!` 流式注册/发现机制（P1 #7）；
  - `StreamEvent`↔Tauri 事件映射表 + `tool_execute_stream` 仅收 `filePath`（P1 #8/#9）。
- **08-data-model.md**：`ToolInput`/`UserConfig`/`GeneralConfig`/`ThemeConfig`/`ThemeMode`(`#[default] Dark`)/`ShortcutBinding` 加 `Default`（P0 #2）；配置/历史/工作区路径统一为 `ProjectDirs::from("dev","qraft","Qraft")`（P0 #5）。
- **09-interface-design.md**：§3.5 错误码补 `ERR_OUT_OF_MEMORY`(507)；§4.4 事件补 `tool_chunk { taskId, text }`（P1 #8）。
- **16-state-management.md**：`ConfigStore` 改为实现 09 的异步 `ConfigStore` trait（P0 #6）；新增 `history_path()` / `workspace_path()` 与 08 路径统一；去重 `use` 段。

## 已确认一致项
- 10-error-handling.md 既有 `EngineError::ToolNotFound → ERR_TOOL_NOT_FOUND` 与 05 新增变体对齐，无需改动；其 `#[async_trait]` 为既有代码示例。
- 11-testing-strategy.md §3.4/§3.5 已用 `ToolInput { ..Default::default() }`、§3.2 用 `UserConfig::default()`，坐实 P0 #2 修正的必要性。

## 遗留 P2（用户未要求本次处理）
- 20 篇 `author: [wait]` 占位补全/删除；
- `03` 中 `hash` 等 crate 版本写 `latest` 需改 `^` 范围；
- `config_get` 返回类型含糊（示例传字符串 vs trait 声明 `Value`）；
- `16` cancel 占位 `'...'` 的 taskId 注明仅适用于流式任务。

## 关键架构决策（长期）
- 配置基目录单一来源：`directories::ProjectDirs::from("dev", "qraft", "Qraft")`，配置/历史/工作区均置于其 `config_dir()` 下。
- trait 异步化：`Tool` / `StreamingTool` / `ConfigStore` 均依赖 `async_trait`。
