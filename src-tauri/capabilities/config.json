{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "config",
  "description": "Capability for config_* commands: get/set/get_all/reset (custom commands, no plugin permissions needed)",
  "windows": ["main", "popout-*"],
  "permissions": []
}
