# 计划:在 qraft 根目录创建 AGENTS.md

## 说明

按你的要求调整:

- **文件内容零外部参考备注**——全文只描述 qraft 自身规范,不出现任何"参考/参照/来自 EcoPaste"之类字样。
- 内容已定稿(起草时即基于本仓库代码核实:package.json、src/tools/registry.ts、src/lib/tool-catalog.ts、src-tauri/src/core/{tool,registry}.rs、store/hooks/lib 目录、CI 配置),批准后一次成文。

## 文件结构(AGENTS.md,根目录,中文,~190 行)

1. **文件头**:单一真相源声明 + 一句话项目定位(本地优先开发者工具箱,Rust 工具引擎 + Tauri v2 + React,无遥测)
2. **快速原则**:Local-First 出网白名单、工具引擎纯函数约束、toolId 三处一致且不可改名(破坏 localStorage 数据)、双语同步(en-locale-sweep 强制)、版本单一来源经 bump-version.sh、已发布数据契约迁移、尊重 dirty worktree、Conventional Commits 中文提交信息
3. **技术栈表**:Tauri v2 / React 19 + TS 6 strict / shadcn(Radix)+ Tailwind v4 CSS-first / Zustand 5 / Rust edition 2024 / Vite 8 + pnpm 9 / ESLint·Prettier·rustfmt·clippy·Vitest 4·cargo test
4. **架构边界**:必须在 Rust(执行引擎三重隔离、后端工具、大文件流式、系统能力、持久化)/ 保留在前端(全部 UI、纯前端工具、渲染须 DOMPurify)/ 跨端契约(ipc.ts 包装、CommandResponse、invoke_handler 集中注册、拖放路径须 fs_authorize_dropped_paths 授权)
5. **新增工具流程**:组件 → registerTool 懒加载 → CatalogEntry(中文源 + EN_TOOLS + Lucide 图标 + 8 分类)→ i18n 双语片段 → 纯前端到止;需 Rust 再加 Tool trait + register_tool! + backendId;流式走 StreamingTool;执行统一 useTool hook
6. **目录约定**:src(components/tools/lib/store/hooks/i18n/pages/types/styles/test)+ src-tauri(core/commands/tools/shell/store/media/net/regex_lab/tests)+ scripts + prd,各目录职责一句话
7. **常用命令**:pnpm dev / tauri dev / build / test / lint / format / typecheck;cargo fmt / clippy -D warnings / test;标注 dev 端口 14200 及原因、dev identifier 数据隔离、CI 检查项
8. **Rust 约定**:Tool trait 纯函数、register_tool!/inventory、ToolError 体系、clippy 严格等级、doc 注释 # Errors/# Panics、release panic="unwind" 勿改
9. **前端约定**:函数组件、inline type import、中文 JSDoc 决策注释、no-explicit-any、zustand 组织与 openTool 唯一入口、i18n fragment 机制、Tailwind v4 CSS-first、cn()、测试 setup.ts 必读、改 IPC 契约双端跑测试
10. **通用代码规范**:文档注释、早返回、空行分组、少注释、不提前抽象、改 UI 实际验证
11. **外部文档**:Tauri / React / Tailwind / shadcn / Zustand / i18next / Vitest 官方链接

## 实施步骤

1. `Write` 创建 `D:\DevTools\project\qraft\AGENTS.md`(上述内容,一次成文)
2. `pnpm exec prettier --write AGENTS.md` 保证 `pnpm format:check` 保持绿
3. 复查文中命令、路径、toolId 拼写与已核实代码一致

## 不做的事

- 不改动任何现有文件,不提交 git
- 文件内不出现任何参考来源备注(包括 EcoPaste 字样)
