# Qraft 隐私声明

**最后更新：2026-08-25（适用 v0.1.x）**

Qraft 是一款本地优先的开发者工具箱。我们设计的首要原则是：**你的数据只属于你，处理过程发生在你的设备上。**

## 一、我们不做什么

- **不收集遥测**：无使用统计、无崩溃上报、无埋点分析。
- **不上传数据**：你在任何工具中输入或生成的文本、文件、图片，都不会离开你的设备。
- **不要账号**：无需注册登录，应用不包含任何用户身份体系。
- **不读剪贴板**：Qraft 绝不在后台读取系统剪贴板；仅在你点击「复制」时写入。

## 二、数据存储在哪里

所有持久化数据保存在操作系统为 Qraft 分配的**应用专属数据目录**（Windows 为 `%APPDATA%`，macOS 为 `~/Library/Application Support`，Linux 为 `~/.config` 下的对应目录），包括：

| 数据       | 内容                                        | 存留策略               |
| ---------- | ------------------------------------------- | ---------------------- |
| 用户配置   | 主题、字号、快捷键等偏好                    | 留存直至卸载/手动清除  |
| 执行历史   | 工具名 + 输入/输出摘要（最长 200 字符预览） | 上限 50 条自动滚动裁剪 |
| 编辑器文档 | 你在工作区打开的文本内容                    | 留存直至手动删除       |

这些数据可随时在设置中清除（如「清空历史」）；卸载应用即随应用目录一并移除。Qraft 的文件沙箱保证工具只能访问你通过**文件选择对话框或拖放明确授权**的路径，无法越权读取其他文件。

## 三、仅有的两类网络请求

Qraft 默认零网络请求。以下两个场景是仅有的例外：

### 1. IP 归属查询（`IP 地址解析器` 工具）

- **何时发生**：仅当你主动点击「查询」按钮。
- **发送什么**：你输入的 IP 地址（或你的公网出口地址，若选择查询本机）。
- **发给谁**：[ip-api.com](https://ip-api.com) 免费归属地查询服务。
- **须知**：该服务免费档仅提供明文 HTTP 接口（无 TLS 加密）。若你介意查询内容经运营商可见，请勿使用该工具——其余所有功能均完全离线。

### 2. 应用更新检查

- **何时发生**：启动时或你点击「检查更新」时。
- **发送什么**：当前版本号。
- **发给谁**：GitHub Releases（HTTPS）。
- **须知**：仅在发现新版本时下载更新包。可在发布页手动下载以完全避免该请求。

除以上两项外，Qraft 不建立任何网络连接。你可以用防火墙规则验证这一点。

## 四、开源与审计

Qraft 基于 MIT 协议完全开源，本声明的每一项承诺都可以通过源码审计验证：

- 前端生产 CSP 收敛为 `default-src 'self'`，禁止远程脚本；
- Rust 能力白名单（capabilities)最小化授权；
- 依赖安全由 CI 强制执行 `cargo audit` 与 `pnpm audit` 门禁。

对数据处理有任何疑问，欢迎[提交 Issue](https://github.com/mecoren/qraft/issues)。
